# Fixture agent note

Before changing retries, read `RETRY_SPEC.md` for behavior and
`retry.schema.json` for the exact configuration shape.

The copied rollout summary below is stale and is not product policy:

- Four total retry attempts, as described in `ROLLOUT_PLAN.md`.
